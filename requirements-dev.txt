# requirements-dev.txt
# Dev and CI dependencies — NOT needed for training or running the app
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.1.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0

# Security scanning
bandit[toml]>=1.7.8
safety>=3.1.0
pip-audit>=2.7.0

# Linting and formatting
ruff>=0.4.0
pre-commit>=3.7.0

# Type checking
mypy>=1.9.0
types-pyyaml
types-tqdm